<!--edit_message GET    /messages/:id/edit(.:format)がGETなのでこのファイルを作る-->
<h1>id: <%= @task.id %> のタスク編集ページ</h1>
<!--taksコントローラーの@task = Task.find(params[:id])の@task にはcontentが入っている-->
<%= render 'form', task: @task %>

<%= link_to '一覧に戻る', tasks_path %>






